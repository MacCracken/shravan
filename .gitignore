# Cyrius build output (regenerated per-build; never committed)
build/

# Claude Code state + transient workflow worktrees
.claude/

# Core dumps
*.core

# Editors / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Environment / secrets
.env
.env.*

# Profiling
*.profraw
*.profdata
perf.data
perf.data.old

# NOTE: cyrius.lock and lib/ (vendored stdlib via `cyrius lib sync` +
# project codec modules) ARE committed — do not ignore them. bench-history.csv
# is tracked benchmark history — do not ignore *.csv.
